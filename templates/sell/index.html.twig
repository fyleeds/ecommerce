{% extends 'base.html.twig' %}

{% block title %}Hello SellController!{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('sell') }}
{% endblock %}

{% block body %}

<header class="header">
  <div class="logo-section">
    <!-- Remplacez 'logo.png' par le chemin de votre image de logo réelle -->
    <a href="{{ path('homepage') }}">
      <img src="{{ asset('img/logo.png') }}" alt="Amiishop Logo">
    </a>
    <span>Amiishop</span>  
  </div>
  <div class="search-section">
    <input type="search" placeholder="Rechercher">
    <button type="submit">🔍</button>
  </div>
  <div class="user-section">
    <!-- Ici, ajoutez vos icônes utilisateur et panier, vous pouvez utiliser des images ou des icônes de polices -->
    <a href="{{ path('cart_index') }}">
      <!-- Insérez le chemin de votre image à la place de "chemin/vers/votre/image.jpg" -->
      <img src="{{ asset('img/sac.png') }}" alt="Votre image" class="utilisateur"/>
    </a>
    <a href="{{ path('my_account_index') }}">
      <!-- Insérez le chemin de votre image à la place de "chemin/vers/votre/image.jpg" -->
      <img src="{{ asset('img/utilisateur.png') }}" alt="Votre image" class="sac"/>
    </a>
  </div>
</header>

<div class="main-content">
  <img src="{{ asset('img/logo.png') }}" alt="Amiishop Logo" class="login-logo">
  <div class="login-section">
    {{ form_start(form) }}

    <div class="form-group">
        {{ form_row(form.title, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_row(form.content, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_row(form.price, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="date-time-container">
        <div class="date-container">
            {{ form_row(form.createdAt.date.day, {'attr': {'class': 'form-control date-element'}}) }}
            {{ form_row(form.createdAt.date.month, {'attr': {'class': 'form-control date-element'}}) }}
            {{ form_row(form.createdAt.date.year, {'attr': {'class': 'form-control date-element'}}) }}
        </div>
        <div class="time-container">
            {{ form_row(form.createdAt.time, {'attr': {'class': 'form-control time-element'}}) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_row(form.attachment, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_row(form.type, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_row(form.game_character, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_row(form.category, {'attr': {'class': 'form-control'}}) }}
    </div>

    <button type="submit" class="but">Submit</button>

  {{ form_end(form) }}

{# <a href="{{ path('homepage') }}" class="but">Clickez ici pour retourner à la page principale</a> #}
  </div>
</div>
{% endblock %}
